<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3><%= category.name %></h3>
    <h3><%= category.description %></h3>
    
    <ul>
      <% items.forEach(item => { %>
        <li>
          <h3><%= item.name %></h3>
          <p><%= item.description %></p>
          <button type="button" onclick="location.href='/items/<%= item.id %>'">View more</button>
        </li>
      <% }); %>
    </ul>
</body>
</html>